#include "contentWrap.h"

// engix::ContentWrap::ContentWrap(const Texture *texture, Vector2d position, Vector2d padding, size_t width, size_t height, Content content, Rect srcRect, Align horizontalAlign, Align verticalAlign)
// : VisualElement(texture, position, width, height, srcRect, horizontalAlign, verticalAlign), _padding(padding), _content(content)
// {
// }

// void engix::ContentWrap::update(const Mouse &mouse)
// {
//     VisualElement::update(mouse);
// }

// void engix::ContentWrap::updateContent()
// {
//     _content.position(_position + _padding);
//     _content.srcRect({_calcPosition, _width, _height});
// }
